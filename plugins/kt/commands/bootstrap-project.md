---
description: Bootstrap a new web project from the kt-monorepo-template (Next.js 16 + Sanity v5 + Tailwind 4 + shadcn/ui)
---

Invoke the `bootstrap-project` skill from the `kt-skills` plugin.

Walk me through the inputs interactively: project slug, display name, one-line description, target directory, design tokens source, GitHub visibility, Sanity project ID + dataset, and Vercel project name. Stop and confirm before each irreversible cloud action (GitHub repo creation, Vercel link, git push).

Reference the canonical workflow in `plugins/kt/skills/bootstrap-project/SKILL.md` for the full step list, status messages, and failure handling.
